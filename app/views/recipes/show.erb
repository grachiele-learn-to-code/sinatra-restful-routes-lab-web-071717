<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Recipe <%= @recipe.id %></title>
  </head>
  <body>
    <h1>Recipe <%= @recipe.id %></h1>
    <h2><%= @recipe.name %></h2>
    <h3>Ingredients</h3>
    <p><%= @recipe.ingredients %></p>
    <h3>Cooking Time</h3>
    <p><%= @recipe.cook_time %> minutes</p>
  </body>
  <form action="/recipes/<%= @recipe.id %>/delete" method="post">
    <input id="hidden" type="hidden" name="_method" value="delete">
    <input id="delete" type="submit" name="submit" value="Delete recipe">
  </form>
</html>
